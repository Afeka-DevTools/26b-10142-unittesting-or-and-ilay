**אני:** תקשיב, יש לי תרגיל בית ואני צריך שתסביר לי קצר ולעניין על כתיבת בדיקות יחידה (Unit Tests) ב-Java. בלי חפירות, תכלס מה צריך לדעת.

**AI:**
בדיקות יחידה (Unit Tests) ב-Java הן בעצם קטעי קוד קטנים שאתה כותב כדי לבדוק קטעי קוד אחרים שלך (לרוב מתודות ספציפיות). המטרה היא לוודא שכל פונקציה עושה בדיוק את מה שהיא אמורה לעשות, וששום דבר לא נשבר כשאתה משנה קוד. 
ב-Java משתמשים בדרך כלל בספרייה שנקראת JUnit. אתה יוצר מחלקת טסטים, ומעל כל פונקציית בדיקה שאתה כותב אתה שם את האנוטציה `@Test`.

**אני:**
אוקיי, ואיך אני בודק תכלס שהתוצאה נכונה? ראיתי משהו על פונקציות Assert.

**AI:**
בדיוק. פונקציות ה-Assert של JUnit הן אלו שבודקות את התוצאה בפועל מול התוצאה שציפית לה. הנה הנפוצות שבהן:
* `assertEquals(expected, actual)` - בודקת האם שני הערכים שווים.
* `assertTrue(condition)` - מוודאת שהתנאי מחזיר אמת (True).
* `assertFalse(condition)` - מוודאת שהתנאי מחזיר שקר (False).
* `assertNull(object)` / `assertNotNull(object)` - בודקות אם אובייקט הוא Null או לא.
* `assertThrows(Exception.class, () -> { ... })` - מוודאת שהקוד זורק שגיאה ספציפית במקרה של קלט לא תקין.

**אני:**
תגיד, ואיך אני יודע שהבדיקות שכתבתי מספקות? איך בודקים שכל הנתיבים של הפונקציה נבדקו?

**AI:**
זאת שאלה מעולה. כדי לדעת אם הבדיקות מספקות, אנחנו משתמשים במושג שנקרא **Code Coverage** (כיסוי קוד). זה בעצם מדד שאומר כמה אחוזים משורות הקוד שלך באמת הופעלו במהלך הטסטים.
כדי לוודא שכל "הנתיבים" נבדקו, אתה צריך להסתכל על משפטי התנאי שלך (if/else, switch). 
אם למשל יש לך פונקציה שיש בה `if (x > 0)` ו-`else`, כדי שהבדיקות יספקו אתה חייב לכתוב לפחות שני טסטים:
1. טסט אחד שבו `x` גדול מ-0 (כדי להיכנס לנתיב הראשון).
2. טסט שני שבו `x` קטן או שווה ל-0 (כדי להיכנס לנתיב השני, ה-else).

בנוסף, חשוב לבדוק **מקרי קצה (Edge Cases)** – למשל, מה קורה כשמכניסים 0, מספרים שליליים, מחרוזת ריקה, או רשימה ריקה. יש כלים שמתלבשים על ה-IDE (כמו JaCoCo שעובד מעולה עם Gradle) ויודעים לצבוע לך בירוק ואדום בדיוק אילו שורות עברו בטסטים ואילו נתיבים פספסת.

**אני:**
סבבה הבנתי. תודה.